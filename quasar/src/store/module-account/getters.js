export const getCmdrName = (state) => {
  return state.CmdrName
}

export const getAccountID = (state) => {
  return state.ID
}

export const getCurrentGameID = (state) => {
  return state.CurrentGameID
}

export const getAuth0 = (state) => {
  return state.Auth0
}
